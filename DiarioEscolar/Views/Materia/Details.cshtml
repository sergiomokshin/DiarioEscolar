@model DiarioEscolar.Models.Materia

@{
    ViewBag.Title = "Detalhes";
}

<h2>Detalhes</h2>

<fieldset>
    <legend>Materia</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Descricao)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Descricao)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Salvar", "Edit", new { id=Model.MateriaId }) |
    @Html.ActionLink("Voltar", "Index", new { id = ViewBag.AnoSerieId })
</p>
